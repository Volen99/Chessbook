import {Encoding} from "tslint/lib/utils";

export class HMACSHA1Generator {
  public ComputeHash(source: number[], key: number[]): number[] {
    // HMACSHA1 hma = new HMACSHA1(key);
    // return hma.ComputeHash(source);

    return null;
  }

  // public ComputeHash(source: string, key: string, encoding: Encoding): number[] {
  //    HMACSHA1 hma = new HMACSHA1(encoding.GetBytes(key));
  //    return hma.ComputeHash(encoding.GetBytes(source));
  //
  // }
}

