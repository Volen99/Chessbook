{
  "extends": "../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "../out-tsc/spec",
    "baseUrl": "./",
    "types": [
      "jasmine",
      "node"
    ]
  },
  "files": [
    "test.ts",
    "src/polyfills.ts" // was without src but in peertube it is with src
  ],
  "include": [
    "**/*.spec.ts",
    "**/*.d.ts"
  ]
}
