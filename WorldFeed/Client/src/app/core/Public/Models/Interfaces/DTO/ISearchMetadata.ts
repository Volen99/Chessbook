export interface ISearchMetadata {
  completedIn: number;
  maxId: number;
  maxIdStr: string;
  nextResults: string;
  query: string;
  refreshURL: string;
  count: number;
  sinceId: number;
  sinceIdStr: string;
}
